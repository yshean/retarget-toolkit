% calculate Euclidean distance between 2 vector
function distance = euclidean_distance(vector1, vector2)
    d = size(vector1);
    distance = 0;
    for i = 1:d(1)
        distance = distance + (vector1(i) - vector2(i))^2;
    end
    distance = sqrt(distance);
end